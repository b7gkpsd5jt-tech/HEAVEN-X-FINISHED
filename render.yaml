services:
  - type: web
    name: heavenx-api
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install --no-frozen-lockfile && npx tsc -p lib/db/tsconfig.json && cd artifacts/api-server && node build.mjs
    startCommand: node artifacts/api-server/dist/index.mjs
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "8080"
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
